# Path to the SQLite file. Defaults to ./tutorflow.db if unset.
DB_PATH=./tutorflow.db

# Required only for generate_progress_report, which calls the Anthropic API
# to draft a parent-friendly summary from session notes.
ANTHROPIC_API_KEY=
